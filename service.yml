apiVersion: v1
kind: Service
metadata:
  name: gochuchamchi-web-svc
  namespace: gochuchamchi
spec:
  type: ClusterIP
  selector:
    app: gochuchamchi-web
  ports:
    - port: 80
      targetPort: 8080
